import 'package:flutter/material.dart';

class ProfileBarScreen extends StatelessWidget {
  const ProfileBarScreen({super.key});

  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Profile"),
    );
  }
}
