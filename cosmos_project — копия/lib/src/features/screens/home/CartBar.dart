import 'package:flutter/material.dart';

class CartBarScreen extends StatelessWidget {
  const CartBarScreen({super.key});

  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Cart bar"),
    );
  }
}
