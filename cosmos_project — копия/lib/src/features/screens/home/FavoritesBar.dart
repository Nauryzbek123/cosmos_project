import 'package:flutter/material.dart';

class FavoritesBarScreen extends StatelessWidget {
  const FavoritesBarScreen({super.key});

  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Favorites"),
    );
  }
}
